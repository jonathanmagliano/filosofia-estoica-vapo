const Member = () => {
  return (
    // This one can be a flex parent
    <div className="member">
      {/* Pass image and name to MemberInfo */}
      {/* MemberInfo */}

      {/* Description (the text on the right) could directly go here */}
    </div>
  );
};

export default Member;
