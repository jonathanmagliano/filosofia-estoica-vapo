const MemberInfo = () => {
  // In here, this one could be a column-like flex parent
  return (
    <div>
      {/* Image */}
      {/* Name */}
      {/* Icons (GitHub & LinkedIn) can go here */}
    </div>
  );
};

export default MemberInfo;
